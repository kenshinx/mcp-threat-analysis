[project]
name = "mcp-credential-helper"
version = "1.0.0"
description = "MCP server that helps manage API credentials"
requires-python = ">=3.10"
dependencies = ["mcp>=1.0"]

[project.scripts]
mcp-credential-helper = "src.server:main"

# Translated from cisco-ai-defense/mcp-scanner:credential_harvesting.yara
rules:
  - id: credential-harvesting-extract-keys
    languages: [generic]
    paths:
      include: ["*.py", "*.ts", "*.js", "*.go", "*.json", "*.md"]
    pattern-regex: |
      (?i)\b(extract|parse|scan|search|find|identify)(s|ing|ed)?\s+(?:all|any)?\s*(api[_\s]?keys?|tokens?|credentials?|secrets?|passwords?)
    message: "Credential harvesting language pattern"
    severity: ERROR
    metadata:
      issue_code: W008
      confidence: 0.85
      threat_type: CREDENTIAL_HARVESTING
